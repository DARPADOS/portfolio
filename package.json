{
  "name": "jlallaucca-portfolio",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "npx astro-i18next generate && astro dev",
    "start": "npx astro-i18next generate && astro dev",
    "build": "npx astro-i18next generate && astro build",
    "preview": "npm run build && wrangler pages dev dist",
    "astro": "astro",
    "test-prod": "npm run build && npx servor dist index.html 3000 --reload --browse --static",
    "publish": "npm run build && wrangler pages publish dist"
  },
  "devDependencies": {
    "astro": "^2.0.10"
  },
  "dependencies": {
    "astro-i18next": "^1.0.0-beta.17",
    "wrangler": "^2.9.0"
  }
}
